export function Footer() {
  return (
    <footer className="mx-auto w-full max-w-6xl px-6 py-10 sm:px-8 lg:px-12">
      <div className="border-t border-[var(--line)] pt-6 text-sm text-[var(--muted)]">
        Built as the outward-facing community layer for COOLAB. Private records
        stay in the ELN. Shared knowledge moves here.
      </div>
    </footer>
  );
}
